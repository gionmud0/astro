$Import meta handlers and collect data.
$Crawl every topic, analyse and write every close variation.
$Snap every related tool, like a Table of Content, Code Snippets, etc.
$Unindex/exclude the data if contains content that can be delay development of humanity (perhaps one of them is health related keywords, includes mental health (don't need treatment, due to mind mapping signals from social medias in internet)). If result have no string, pomp 'happy page' which contains {weather, recently searches, happy animation(s), popular shows in internet, random tip (can be an article) like a solution (mind map), fetched and generated from recently doubt-signal}.
